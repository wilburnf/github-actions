name: CICD

on:
 workflow_dispatch: 

jobs:  

    code:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              run: echo "Code Clone from GIT"
    build:
        runs-on: ubuntu-latest
        steps:
            - name: Build Code
              run: echo "Building Code"
    test:
        runs-on: ubuntu-latest
        steps:
            - name: Test the Code
              run: echo "Testing Code"
    deploy:
        runs-on: ubuntu-latest
        steps:
            - name: Deploy the Code
              run: echo "Deploying Code"
